<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Écran Admin</title>
    <!-- Bootstrap CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .etat {
            font-size: 0.9rem;
            padding: 5px 10px;
            border-radius: 5px;
            font-weight: bold;
            display: inline-block;
        }
        .a-traiter { background-color: #fdf2b2; color: #6c6400; }
        .valide { background-color: #daf5cd; color: #2e6b00; }
        .rejete { background-color: #f5b2b2; color: #850000; }
    
        /* Style du bouton "Consulter" */
        .btn-consulter {
            background-color: #2F5597;
            color: white;
            border: none;
            padding: 5px 10px;
            border-radius: 5px;
        }
        .btn-consulter:hover {
            background-color: #244172; /* Un bleu plus foncé au survol */
        
        }</style>
</head>
<body >

    <!-- En-tête -->

    <!-- Bouton "Mon Espace" -->
    <div class="d-flex justify-content-end p-3">
        <button class="btn text-white"  style="background-color: #2F5597;">Mon Espace</button>
    </div>

    <!-- Barre de recherche -->
    <div class="container">
        <h3>Liste des utilisateurs</h5>
            <div class="search-container d-flex align-items-center gap-3">
                <button class="btn text-white" style="background-color: #2F5597;">Gestion des comptes</button>
                <button class="btn text-white" style="background-color: #2F5597;">Formulaires organisations</button>
                <button class="btn text-white" style="background-color: #2F5597;">Formulaires recruteurs</button>   
            </div>

        <!-- Liste des offres -->
        <div class="mt-4 bg-white p-3 rounded shadow">
            <table class="table">
                <thead>
                    <tr>
                        <th>Adresse mail</th>
                        <th>Nom</th>
                        <th>Prénom</th>
                        <th>Type</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                <% users.forEach((user)=> { %>
                <tr>
                <td>
                <%= user.mail %>
                </td>
                <td>
                <%= user.nom %>
                </td>
                <td>
                <%= user.prénom %>
                </td>
                <td>
                    <%= user.type_utilisateur %>
                    </td>
                </tr>
                <% }) %></tbody>
            </table>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
