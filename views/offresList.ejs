<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Écran Recruteur</title>
    <!-- Bootstrap CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: white;
        }
        .header {
            background-color: #e0e0e0;
            padding: 10px 20px;
            font-weight: bold;
        }
        .search-container {
            background-color: #2F5597;
            padding: 15px;
            border-radius: 8px;
            display: flex;
            gap: 10px;
        }
        .search-container input,
        .search-container select {
            flex: 1;
        }
        .etat {
            font-size: 0.9rem;
            padding: 5px 10px;
            border-radius: 5px;
            font-weight: bold;
            display: inline-block;
        }
        .non-publiee { background-color: #fdf2b2; color: #6c6400; }
        .en-cours { background-color: #f7e49c; color: #6c6400; }
        .publiee { background-color: #daf5cd; color: #2e6b00; }
        .expiree { background-color: #f5b2b2; color: #850000; }
    </style>
</head>
<body >

    <!-- En-tête -->

    <!-- Bouton "Mon Espace" -->
    <div class="d-flex justify-content-end p-3">
        <button class="btn text-white"  style="background-color: #2F5597;">Mon Espace</button>
    </div>

    <!-- Barre de recherche -->
    <div class="container">
        <h5>Vos offres :</h5>
        <div class="search-container d-flex align-items-center">
            <input type="text" class="form-control" placeholder="Nom de l'offre">
            <select class="form-select">
                <option selected>État de l'offre</option>
                <option value="non-publiee">Non publiée</option>
                <option value="en-cours">En cours</option>
                <option value="publiee">Publiée</option>
                <option value="expiree">Expirée</option>
            </select>
                <button type="submit" class="btn btn-light"> Rechercher</button>
            </button>
        </div>

        <!-- Liste des offres -->
        <div class="mt-4 bg-white p-3 rounded shadow">
    <table class="table">
        <thead>
          <tr>
            <th>numéro</th>
            <th>état</th>
            <th>mailRecrut</th>
          </tr>
        </thead>
        <tbody>
          <% offres.forEach(offre => { %>
            <tr>
              <!-- Numéro avec couleur bleue -->
              <td class="blue-column"><%= offre.numéro %></td>
      
              <!-- État avec couleur -->
              <td class="blue-column">
                <% if (offre.etat === 'publiée') { %>
                  <span class="etat publiee"><%= offre.etat %></span>
                <% } else if (offre.etat === 'non publiée') { %>
                  <span class="etat non-publiee"><%= offre.etat %></span>
                <% } else if (offre.etat === 'expirée') { %>
                  <span class="expiree"><%= offre.etat %></span>
                <% } else if (offre.etat === 'en cours d’édition') { %>
                <span class="etat en-cours"><%= offre.etat %></span>
                <% } else { %>
                  <span class="etat"><%= offre.etat %></span>
                <% } %>
              </td>
      
              <!-- Date de validité avec couleur bleue -->
              <td class="blue-column"><%= offre.mailRecrut%></td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
</div>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html> 
      
</body>
